# 품질관리 보고서

**날짜**: 2026-04-03
**작업**: 디자인 토큰 중앙화 및 타이포그래피 개선

## 검토 요약

- 검토 파일: 13개
- 발견 이슈: 6건 (심각 0 / 경고 4 / 참고 2)
- 수정 완료: 6건 (전체 파일 리팩토링)

## 발견 및 수정 내역

### [경고] 하드코딩 색상 값 전체 범위
- **파일**: `AuthCard.tsx`, `LoginForm.tsx`, `RegisterForm.tsx`, `Dashboard.tsx`, `AdminShell.tsx`, `admin/page.tsx`, 어드민 5개 페이지
- **문제**: `#d4af37`, `#c9a84c88` 등 색상 리터럴이 각 파일에 산재하여 중앙 관리 불가
- **수정**: `src/lib/theme.ts` (C, F, cardStyle 등)에서 import하도록 전체 교체

### [경고] 중복 별/네뷸라 렌더링 로직
- **파일**: `AuthCard.tsx`, `AdminShell.tsx`
- **문제**: 동일한 Star 인터페이스, useEffect 생성 로직, CSS keyframe이 두 파일에 복제
- **수정**: `src/components/StarBackground.tsx`로 추출하여 두 파일 모두에서 사용

### [경고] 11~12px 소형 폰트
- **파일**: `AdminShell.tsx` (th: fontSize 11), `admin/page.tsx` (label fontSize 11, 12), `admin/dashboard/page.tsx` (stat label 11), `participations/page.tsx` (summary label 10)
- **문제**: 11px 이하 폰트는 가독성 저하, 특히 한국어 환경에서 심각
- **수정**: 모든 본문 레이블 `F.xs(13px)` 이상으로 상향 조정

### [경고] AdminShell `export { inputStyleExport as input }` 구문
- **파일**: `AdminShell.tsx` (수정 후 초기 버전)
- **문제**: `const` 선언 후 동일 이름 재export 시 혼란 및 불필요한 중간 변수
- **수정**: `export const input: React.CSSProperties = inputStyle`로 단순화

### [참고] Dashboard 배경 `#050710` 단색
- **파일**: `Dashboard.tsx`
- **문제**: 어드민/인증 페이지는 별 배경을 쓰지만 사용자 대시보드만 단색 배경으로 시각적 일관성 부재
- **수정**: `StarBackground` 컴포넌트 사용, 단 네비바와 콘텐츠에 `position: relative, zIndex: 1` 추가하여 레이어 순서 보장

### [참고] 어드민 테이블 첫 셀 정렬
- **파일**: `members/page.tsx`, `alliances/page.tsx`, `events/page.tsx`, `participations/page.tsx`
- **문제**: 테이블 첫 번째 td가 좌측 카드 경계에 붙어 시각적 답답함
- **수정**: 첫 번째 td에 `paddingLeft: 24px` 추가

## 신규 파일

| 파일 | 내용 |
|------|------|
| `/src/lib/theme.ts` | 색상(C), 타이포그래피(F), 재사용 스타일 객체(cardStyle, thStyle, tdStyle, goldBtn 등) |
| `/src/components/StarBackground.tsx` | 배경 그라디언트 + 네뷸라 4레이어 + 별 애니메이션 통합 컴포넌트 |

## 수정된 파일

| 파일 | 변경 내용 |
|------|-----------|
| `src/components/AuthCard.tsx` | 별 로직 제거, StarBackground 사용, theme 색상 적용 |
| `src/components/LoginForm.tsx` | theme import, 폰트 사이즈 조정 |
| `src/components/RegisterForm.tsx` | theme import, 폰트 사이즈 조정 |
| `src/components/Dashboard.tsx` | 단색 배경 제거, StarBackground 사용, theme 적용, 닉네임 F.xl(22px) |
| `src/app/admin/_components/AdminShell.tsx` | 별 로직 제거, StarBackground 사용, theme re-export, 사이드바 폰트 15/14px, main maxWidth 1400px |
| `src/app/admin/page.tsx` | 배경 레이어 제거, StarBackground 사용, theme 적용 |
| `src/app/admin/dashboard/page.tsx` | sectionTitle/cardTitle 적용, C/F theme 사용 |
| `src/app/admin/members/page.tsx` | sectionTitle 적용, F.xs 폰트 조정, 첫 td paddingLeft 24px |
| `src/app/admin/alliances/page.tsx` | sectionTitle 적용, F.xs 폰트 조정, 첫 td paddingLeft 24px |
| `src/app/admin/events/page.tsx` | sectionTitle 적용, F.xs 폰트 조정, 첫 td paddingLeft 24px |
| `src/app/admin/events/[id]/participations/page.tsx` | sectionTitle 적용, C.success/C.danger 사용, 첫 td paddingLeft 24px |

## 빌드 결과

```
✓ Compiled successfully in 1199ms
✓ Generating static pages using 7 workers (9/9)
```

TypeScript 에러 0건, 빌드 에러 0건.

## 잔여 리스크

- `Dashboard.tsx`의 `ChangePasswordForm`, `UploadForm`은 이번 리팩토링 범위 밖이므로 내부적으로 하드코딩 색상이 남아 있을 수 있음. 필요 시 별도 작업 권장.
- 어드민 모달 배경 색(`#0f1730`)은 theme의 `C.bgModal`(`#0f1a38ee`)과 약간 다름. 모달 배경은 반투명 오버레이와 별도로 관리되므로 시각적 영향은 미미하나 추후 통일 검토 가능.
